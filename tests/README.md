# Tests

Current tests remain at the top level of this directory for compatibility with
the existing `python3 -m unittest ...` commands.

The `unit/`, `integration/`, and `wolfram/` subdirectories are reserved for a
future test split once imports and discovery are adjusted intentionally.
